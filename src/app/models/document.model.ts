export interface Document {
    id?: number;
    name?: string,
    type?: string,
    typeDoc?: string,
    size?: string,
    statut?: string,
    mot_cles?: string,
    fileContent?: any,
    docPath?: string,
}